# Chatbot-RAG

This is a chatbot that uses a generative model to produce answers based on retrieved clauses from regulations. The chatbot is built using the Hugging Face Transformers library and uses the Mistral AI model for generation. The retrieved clauses are scored using a combination of faithfulness, citation, and relevance.

## Installation

To install the chatbot, run the following command:
